当前进度：
批处理编程结构基本固定

TODO：
EvalLater 封装
垃圾回收统一使用RefCount
闭包分析内部对外的隐式引用（遍历AST），引用计数+1
( ( (fn* (a) (fn* (b) (+ a b))) 5) 7)
释放Fn时同时释放隐式引用

垃圾回收思路2
全部复制

更改字符串数据结构 & 处理逻辑 ： 
换行与不换行，末尾是否有换行需要清晰化+封装
换行的表示
